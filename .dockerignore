# Git
.git
.gitignore

# Build / deps — rebuilt inside the image
node_modules
.next
out
npm-debug.log*

# Other apps / dev leftovers in this repo.
# NOTE: mbo_youth_emp is intentionally NOT excluded — Coolify imports the whole
# repo for the compose deploy (backend/worker build contexts need it). The
# frontend image stays lean because its Dockerfile copies only src/public/configs.
testone
trash

# Local / editor junk
certificates
.claude
.impeccable
*.md
.env
.env*.local
deploy